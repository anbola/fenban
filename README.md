# 第三小学学生成绩核算系统

> 第三小学学生成绩核算系统 by 冬阳 v1.42

## 在线使用

直接访问 [成绩核算系统](https://anbola.github.io/chengji/) 即可使用。

## 文件说明

```
成绩核算系统_v1.39_完整包/
├── deploy/                    # 部署文件夹（上传到GitHub Pages）
│   ├── index.html             # 加密部署版（含源码保护）
│   └── logo_.ico              # 网站图标
├── source_v1.42.html          # 源代码（未加密版）
├── 提示词文档.html            # 完整功能说明文档
├── build_deploy.py            # 构建脚本（从源码生成加密部署版）
├── logo_.ico                  # Logo 图标源文件
└── README.md                  # 本文件
```

## v1.42 更新内容

### 1. 参数修改确认按钮（防误操作）
- **积分比例、档位比例、奖励比例修改需点击确认按钮**：修改后按钮变红闪烁提示
- 未确认参数时，核算和导出操作会被阻止并提示
- 已有核算结果时确认参数会提示重新核算

### 2. 学科总分工作表"科任教师"列改为"班主任"
- 学科总分工作表的第19列表头从"科任教师"改为"班主任"
- 数据填入班主任姓名（其他科目工作表保持"科任教师"不变）

### 3. 默认打开"语文"学科工作表
- 导出的Excel工作簿默认激活"语文"工作表标签页
- 方便用户第一时间查看语文科目核算结果

### 4. 科任教师工作表移到最后
- 科任教师工作表从原来的中间位置移到所有工作表的最后
- 工作表顺序：成绩 → 各科目 → 获奖教师 → 获奖学生 → 科任教师

### 5. 一科也可计算提示
- **使用说明中红色加粗强调**：虽然语文、数学、英语为必填项，但实际只需填入任意一科成绩即可核算
- **模板悬浮提示**：语文、数学、英语列批注添加"一科也可计算"说明

### 6. 工作簿重点内容加粗加色
- 人均分、人均积分、A档人数、A档积分、B档人数、B档积分、总积分等关键列数据
- 加粗并显示为红色，醒目突出

## v1.41 更新内容

### 1. 不计成绩标记改为Q字母
- **从"*"改为"Q"字母**：更易理解，避免Excel数据丢失
- 解析和导出逻辑同步更新，兼容Q/q/*等多种输入

### 2. 导入模板必填项红色*标记
- **必填项表头添加红色"*"号**：班级、姓名、语文、数学、英语
- 必填项表头文字也显示为红色，醒目提示

### 3. 非必填项悬浮提示
- **非必填项批注增加"非必填"字样**：总分、年名、科任教师、场地、考场、考号、身份证号码、道法、科学等
- 鼠标悬停表头即可查看提示

### 4. 考试名称弹窗确认
- **导入文件后自动弹窗**：显示识别到的考试名称，用户可确认或修改
- 未识别到考试名称时提示用户手动输入
- 考试名称填入核算参数配置中的"考试名称（自动识别，可修改）"输入框

### 5. 使用说明增强
- **新增字段说明部分**：必填项和非必填项列表，红色加粗显示
- **不计成绩标记说明**：明确使用字母"Q"标记
- **必填项提示**：使用说明中红色加粗提示必填项规则

### 6. 模板批注完善
- 所有非必填项列添加批注提示，包含"非必填"字样
- 不计成绩学生列批注更新为"请填入Q"

## v1.40 更新内容

### 1. Logo 修复
- favicon 和页头校徽改用 `logo_.ico`
- 添加 `<link rel="icon">` 确保浏览器标签页显示图标

### 2. 导入模板姓名列锁定
- 模板定制中"姓名"列默认锁定（`locked: true`），防止误删

### 3. 班级名称按原样保留
- 导入模板中班级名是什么，核算表中就显示什么（不再去除"班"字）
- 例：导入"三年级1班" → 核算表显示"三年级1班"
- 教师带多班时合并名如"三年级1班三年级2班"
- 内部匹配使用 `normCls()` 去末尾"班"字进行教师表匹配，显示用原名

### 4. 核算参数配置界面紧凑化
- 改为 CSS Grid 多列自适应布局（`repeat(auto-fit, minmax(220px, 1fr))`）
- 考试名称占整行，其他参数三列并排
- 屏幕窄时自动转为单列

### 5. 成绩工作表增强
- **保留所有学生数据**：含不计成绩学生，保留原始"Q"标注
- **新增备注列**：保存不计成绩原因等备注信息
- **删除年名后的"科任教师"列**：避免与各科目后的科任教师列重复
- **每个科目后新增一列科任教师**（默认隐藏）：检索本学科实际科任教师
- **身份证A列隐藏**：保护学生隐私信息
- **表头悬浮提示**：身份证A列和各科目教师列表头添加批注，说明数据已隐藏及解锁方法
- **隐藏列说明移至右侧空白列**：打印时自动隐藏，屏幕可见

### 6. 不计成绩标记优化（v1.41已改为Q字母）
- **从"0"改为"*"符号**：避免Excel将"0"识别为数字零导致数据丢失
- 解析和导出逻辑同步更新，确保不计成绩学生数据完整保留
- **v1.41更新**：进一步改为"Q"字母，更易理解

### 7. 考试名称智能填入
- **结合文件名和表头智能识别**：自动提取年级、学期、考试类型等信息
- **避免与表头内容重复**：排除普通列名（如"班级"、"语文"等）
- **用户可手动修改**：识别结果填入输入框，用户可自由编辑

### 8. UI界面美化（现代简约风）
- **渐变背景**：头部使用三段渐变蓝色背景
- **卡片阴影**：使用更柔和的阴影效果
- **按钮渐变**：所有按钮使用渐变背景和悬浮效果
- **统计卡片**：顶部添加彩色装饰条
- **卡片标题**：左侧添加蓝色装饰条
- **Logo动画**：悬浮时旋转放大效果

### 9. 导入模板右侧流程说明
- **5步流程图**：下载模板→导入数据→配置参数→开始核算→导出Excel
- **打印隐藏**：设置打印区域排除说明列，屏幕可见

### 10. QQ联系方式
- 主界面页脚添加"软件如有bug，请联系QQ:903286351反馈"

### 11. 提示词文档重构
- 改为单列布局便于阅读
- 新增完整源代码展示
- 新增 GitHub Pages 部署详细教程

## 功能说明

- 导入Excel成绩数据（智能识别工作表）
- 自动核算人均积分、A/B档分数线、积分比率
- 支持多科目核算
- 导出完整Excel报表
- 教师奖励和学生奖励统计
- 科任教师分组核算

## 部署方法

### 方法一：手动部署

1. 将 `deploy/` 文件夹中的所有文件上传到GitHub仓库
2. 在仓库 Settings → Pages 中选择 main 分支
3. 访问 `https://用户名.github.io/仓库名/`

### 方法二：使用构建脚本

1. 修改 `source_v1.40.html` 源代码
2. 运行 `python build_deploy.py` 生成加密部署版
3. 将 `deploy/` 文件夹内容上传到GitHub

## 源码保护

| 操作 | 行为 |
|------|------|
| 右键菜单 | 禁用 |
| F12 | 弹窗要求密码 |
| Ctrl+U | 弹窗要求密码 |
| Ctrl+Shift+I/J | 弹窗要求密码 |
| Ctrl+S | 弹窗要求密码 |
| 域名验证 | 仅允许指定域名 |

## 版权

Copyright 2026 冬阳
